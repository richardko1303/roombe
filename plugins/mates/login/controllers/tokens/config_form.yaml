# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Token

# Model Form Field configuration
form: $/mates/login/models/token/fields.yaml

# Model Class name
modelClass: Mates\Login\Models\Token

# Default redirect location
defaultRedirect: mates/login/tokens

# Create page
create:
    title: backend::lang.form.create_title
    redirect: mates/login/tokens/update/:id
    redirectClose: mates/login/tokens

# Update page
update:
    title: backend::lang.form.update_title
    redirect: mates/login/tokens
    redirectClose: mates/login/tokens

# Preview page
preview:
    title: backend::lang.form.preview_title
